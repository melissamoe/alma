RewriteEngine On

# Si le fichier ou dossier existe vraiment, on le laisse accessible
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Sinon, on envoie la requête vers index.php
RewriteRule ^ index.php [QSA,L]